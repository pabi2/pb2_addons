<?xml version="1.0" encoding="utf-8"?>
<openerp>
    <data>
        <record id="invoice_supplier_form" model="ir.ui.view">
            <field name="name">account.invoice.supplier.form</field>
            <field name="model">account.invoice</field>
            <field name="inherit_id" ref="account.invoice_supplier_form"/>
            <field name="arch" type="xml">
                <xpath expr="//field[@name='check_total']" position="after">
                    <field name="is_advance_clearing" readonly="1"/>
                </xpath>
                <xpath expr="//button[@name='invoice_cancel']" position="after">
                    <!--button name="clearing_invoice_cancel" 
                            string="Cancel Invoice"
                            type="workflow"
                            attrs="{'invisible': ['|', ('state', '!=', 'paid'),('is_advance_clearing', '=', False)]}"
                            groups="account.group_account_invoice"/-->
                    <button name="%(account_cancel_with_reversal.act_account_move_reverse_invoice)d" 
                            type="action"
                            string="Cancel Invoice"
                            attrs="{'invisible': ['|', ('state', '!=', 'paid'),
                                                       ('invoice_type', 'not in', ('expense_advance_invoice', 'advance_clearing_invoice'))]}"
                            class="oe_highlight" 
                    />
                </xpath>
            </field>
        </record>
	
	    <record id="invoice_form" model="ir.ui.view">
	        <field name="name">invoice.form</field>
	        <field name="model">account.invoice</field>
	        <field name="inherit_id" ref="account.invoice_form"/>
	        <field name="arch" type="xml">
	            <xpath expr="//field[@name='fiscal_position']" position="after">
	            	<field name="ref_employee_id" invisible="1" />
	                <field name="advance_expense_id" string="Advance Expense"
	               		domain="[('is_employee_advance', '=', True), ('state', '=', 'paid'),
                                 ('employee_id', '=', ref_employee_id), ('amount_to_clearing', '&gt;', 0.0)]" />
	            </xpath>
	        </field>
	    </record>
    </data>
</openerp>
