<?xml version="1.0" encoding="utf-8"?>
<openerp>
    <data>
        <record id="view_vendor_receipt_form" model="ir.ui.view">
            <field name="name">view.vendor.receipt.form</field>
            <field name="model">account.voucher</field>
            <field name="inherit_id" ref="account_voucher.view_vendor_receipt_form"/>
            <field name="arch" type="xml">
                <field name="journal_id" position="after">
                    <field name="intransit" invisible="0"/>
                    <field name="bank_receipt_id" invisible="0"/>
                </field>
                <button name="cancel_voucher" position="after">
                    <button name="%(action_voucher_create_bank_receipt)d" type="action" string="Create Bank Receipt"
                        attrs="{'invisible': ['|', ('state', '!=', 'posted'), ('intransit', '=', False)]}"/>
                </button>
            </field>
        </record>
    </data>
</openerp>
